package com.sitecake.contentmanager.client.properties;

/**
 * The specification of all possible scopes properties can have.
 */
public enum PropertyScope {
	/**
	 * A scope bound to a single editing session.
	 */
	SESSION,
	
	/**
	 * An application-wide scope.
	 */
	APPLICATION
}
